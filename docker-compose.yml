services:
  postgres-db:
    image: postgres:14
    container_name: able-care-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: able_care_db
    ports:
      - "5432:5432"
